// Browser stub for @resvg/resvg-js.
// The real package is a Node.js native addon used only for server-side PNG
// rendering. Browsers never call it, so an empty stub satisfies the import.
export class Resvg {}
export function initWasm() {}
